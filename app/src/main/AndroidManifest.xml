<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <application
        android:name=".application.Application"
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@drawable/logo"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/Theme.SaleClothesApplication"
        tools:targetApi="31">
        <activity
            android:name=".activity.RoutingActivity"
            android:exported="true"
            android:theme="@style/MySplashScreen">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity android:name=".activity.LoginActivity"
            android:exported="false"/>

        <activity android:name=".activity.MainActivity"
            android:exported="false"/>

        <activity android:name=".activity.LogupActivity"
            android:exported="false"/>

        <activity android:name=".activity.ClothesInfoActivity"
            android:exported="false"/>

        <activity android:name=".activity.OrderInfoActivity"
            android:exported="false"/>

        <activity android:name=".activity.EditInfoActivity"
            android:exported="false"/>
    </application>

</manifest>